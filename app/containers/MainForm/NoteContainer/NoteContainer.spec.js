import React from 'react'
import NoteContainer from './'

describe('NoteContainer container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
